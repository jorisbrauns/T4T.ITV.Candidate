import { FinishedComponent } from './finished.component';
import { Routes } from '@angular/router';

// Route Configuration
export const FinishedRoutes: Routes = [
  { path: 'finished/:id', component: FinishedComponent }
];